<div align="center">

# 📖 SteamEdge 教學

[English](./TUTORIAL.md) · [Türkçe](./TUTORIAL_TR.md) · [Deutsch](./TUTORIAL_DE.md) · [Español](./TUTORIAL_ES.md) · **繁體中文** · [Русский](./TUTORIAL_RU.md)

[回到 README](./README_ZH.md) · [更新紀錄](./CHANGELOG.md)

</div>

---

## 📑 目錄

- [概觀](#-概觀)
- [安裝](#-安裝)
- [介面導覽](#️-介面導覽)
- [功能參考](#-功能參考)
- [設定參考](#️-設定參考)
- [疑難排解](#-疑難排解)
- [常見問題](#-常見問題)
- [名詞解釋](#-名詞解釋)

---

## 🔭 概觀

### 它做什麼

SteamEdge 讓你的 Steam 遊戲保持「執行中」，卻不必真的執行。它收集集換式卡牌、累積遊玩時數、讀寫成就，並依真實市場為你的庫存定價。這些事情通常都需要開著 Steam 客戶端；在這裡一件也不用。

### 運作方式

應用程式使用 Steam 自己的網路協定，與客戶端所用的相同。它以工作階段權杖登入，告訴 Steam 目前正在遊玩哪些遊戲，並讀回徽章頁面、庫存、市集資料與成就結構描述。

由此衍生出兩點，也解釋了這個應用程式大部分的行為：

- **Steam 是唯一的事實來源。** 沒有任何數字是推估或編造的。若某個數字取不到，欄位會顯示破折號，而不是猜測值。
- **Steam 的限制就是應用程式的限制。** 市集請求每個帳號大約限制在 30 秒 20 次，而應用程式中所有接觸市集的部分共用這一份額度。遊戲必須累積滿兩小時總時數才會開始掉卡。這些是實測結果，不是設定選項。

### 檔案配置

所有東西都放在執行檔旁邊。不會寫入登錄檔、`AppData` 或 `Program Files`。

```
SteamEdge/
  SteamEdge.exe
  settings/
    settings.json              一般設定
    accounts.json              已儲存的帳號
    session.json               目前的工作階段權杖
    accounts/<steamID>.json    各帳號專屬：佇列、預設、統計
  cache/
    prices.json                市集價格，有效期 24 小時
    history.json               成交平均價，有效期 72 小時
    basarimsiz.json            確認沒有成就的遊戲
    steamedge.log              回報問題時要附上的紀錄檔
```

> **敏感的是 `settings/`。** `session.json` 裡的權杖足以使用你的帳號。不要放進你會分享的備份、上傳的壓縮檔或截圖裡。

---

## 📦 安裝

### 系統需求

Windows 10 以上，64 位元。已啟用 Steam 兩步驟驗證的 Steam 帳號。解壓縮後約需 320 MB 磁碟空間。不需要 Steam 客戶端，也永遠不會啟動它。

### 逐步說明

1. 從[發行頁面](https://github.com/Miabeyefendi/SteamEdge/releases/latest)下載最新的 `.rar`。
2. 解壓縮到你有權限的資料夾。不要放在 `Program Files`，因為應用程式會把設定寫在自己旁邊。
3. 執行 `SteamEdge.exe`。
4. 登入。QR 分頁比較簡單：用 Steam 手機應用程式掃描並核准。密碼分頁則需要帳號、密碼與一組兩步驟驗證碼。

### 確認安裝成功

工作階段建立後，左下角會顯示「系統：執行中」，右上角的帳號標章會填入你的名稱、頭像與等級。若標章一直空白，表示工作階段沒有建立起來，請看[疑難排解](#-疑難排解)。

### 更新

應用程式會查詢已發行的版本號，有新版時通知你。它刻意不下載也不安裝任何東西。要更新時，把新的壓縮檔解壓縮覆蓋舊資料夾，並保留 `settings/` 與 `cache/`。

### 移除

刪除資料夾。整個程序就這樣。

---

## 🖥️ 介面導覽

### 總覽

首頁。**進行中工作**面板顯示真正在執行的工作，一次一項；同時有多項時可用箭頭切換。開始、停止與詳情都作用於你當下看到的那一項工作，而不是固定的某一頁。

下方是佇列與活動紀錄。右側是卡牌、遊戲庫規模、庫存價值、時數提升與成就的統計方塊。某個方塊在對應頁面尚未載入前會顯示破折號；那說明的是「已取得什麼」，而不是你的帳號狀況。

### 卡牌收集

從你的徽章頁面讀出、仍有卡可掉的遊戲清單。選擇模式、設定時長、按下開始。

### 庫存與市集

你的 Steam 庫存，重複物品會合併成一列。價格與成交平均價在背景取得，一次一件物品、兩個數值一起取。詳情面板會顯示掛單簿，也可以把物品掛上出售。

### 時數提升

可搜尋的完整遊戲庫。選擇遊戲，設定同時數量上限與時長，按下開始。可選的時數同步會把選取的遊戲拉到共同的總時數。

### 擬真模式

三欄式工作區。左邊是佇列與時段長度，中間是解鎖順序，右邊是設定，分為「簡易」與「進階」兩個面板。

### 成就

以遊戲為單位，顯示從協定讀來的真實鎖定與解鎖狀態。可以選取成就後批次解鎖或重新鎖定，進度即時顯示，停止按鈕即使在等待中途也立即生效。

### 設定

所有能對應用程式下達的指示，分組排列：一般、卡牌收集、市集、時數提升、擬真模式、隱私、帳號識別、備份。

---

## 🧩 功能參考

### 卡牌收集

遊戲必須累積滿**兩小時**總時數，Steam 才會開始掉卡。除了一種模式之外，其餘都不管這件事、只是單純執行遊戲；**快速模式**知道這條規則，只輪替已經超過門檻的遊戲，避免把時間浪費在還不可能掉卡的遊戲上。

各種模式：

| 模式 | 作用 |
|---|---|
| 逐一 | 依清單順序，一次一款 |
| 卡最多 | 剩餘卡牌最多的遊戲優先 |
| 卡最少 | 最接近收完的遊戲優先 |
| 優先順序 | 由你自訂順序 |
| 快速 | 只選已超過兩小時的遊戲，以短間隔輪替 |

卡牌不會按表操課，Steam 也不會送出「掉了一張卡」的事件。應用程式會定期量測剩餘卡牌總數，回報真實的差額，而不是憑空生出一個計數器。

### 時數提升

最多同時執行 32 款遊戲。Steam 會分別為每一款開啟中的遊戲計時，所以 32 款遊戲開一小時就是 32 小時的遊玩時數。

**時數同步**會把選取的遊戲拉到相同的總時數。有兩種方式：

- **全部同時** - 所有選取的遊戲同時執行，達到目標的就退出。這是最快的方式：整份工作所需的時間，等於落後最多的那款遊戲。
- **逐一進行** - 落後最多的遊戲單獨先行，追上下一款之後兩者一起繼續。比較慢，但過程中各遊戲能保持齊頭。

進度條建立在共同的時間軸上：一條進度條等於「一減去該遊戲的剩餘時間除以整份工作的長度」。在三十五小時的工作中四小時就完成的遊戲，一開始的進度條幾乎是滿的；而要跑到最後的遊戲則從空的開始。每一條都會在該遊戲達成目標的那一刻剛好到 100%。

### 成就

成就是透過協定讀寫的，不是抓取你的公開個人檔案。個人檔案設為私密不影響任何事。

有兩類成就無法更動，應用程式會從結構描述辨識它們，而不是反覆嘗試失敗：

- **受保護的成就**由遊戲伺服器寫入。Steam 會拒絕任何嘗試更動的客戶端。
- **不透過此協定保存統計的遊戲**（部分大型多人遊戲）會回報 `0 / N`。這是正確結果，不是錯誤。

有些遊戲只在遊戲開啟時才接受成就寫入。應用程式會為了寫入而開啟該遊戲，完成後再還原成先前正在執行的內容。

### 擬真模式

保持單一遊戲開啟，並在整個時段內由最常見到最稀有依序解鎖成就。重點在於它留下的痕跡：一分鐘內冒出數百個成就，在個人檔案與第三方網站上都非常顯眼。

**100% 完成時數**是整個頁面據以計算的數字：把這款遊戲連同所有成就完成需要幾小時。填入之後會針對該遊戲記住。留空則依遊戲類型推估，但該推估是你自己的遊玩時數乘上一個係數，因此在你玩很久的遊戲上會偏高。

**目標數量**由兩部分組成：以你的遊玩時數而言「本來應該已解鎖的數量」減去「實際已解鎖的數量」，再加上本次時段自身應得的份額。面板會把算式寫出來，方便你核對。

**分佈模型**決定間隔的形狀。線性是平均分佈；指數在前段密集，像真實玩家最初幾小時的樣子；帕雷托則把大多數集中在前五分之一。

**節奏**依稀有度加權。只有低於 5% 的成就會明顯等更久，其餘一律保持平穩而快速的節奏。若遊戲的遊玩時數已超過完成時數，整份排程會壓縮，因為已經沒有學習曲線可以模擬了。

**沒有成就的遊戲**一經確認就會從佇列移除，寫入 `cache/basarimsiz.json`，且不再於此頁面出現。Steam 公布的遊戲庫旗標並不可靠，只有結構描述請求才可靠。

### 庫存與市集

物品價值是**成交紀錄的數量加權中位數**，而不是最低的現有掛單。單一使用者把一張卡掛到 999,999 並不會影響它。

價格以你帳號的**錢包貨幣**傳來，並原封不動顯示。刻意不做換算：換算就等於自行編造匯率。

價格與成交平均價是**以物品為單位一起取得**的，然後佇列才前進到下一件。兩者共用 Steam 唯一的市集額度，且限制是以請求次數計算，而非物品數量。

### 多帳號

可以同時連線多個帳號。每個帳號各自維持自己的引擎、佇列與資料檔。切換帳號不會重啟應用程式，也不會中斷其他帳號正在做的事。

---

## ⚙️ 設定參考

設定存放在 `settings/settings.json`。以下全部都能從「設定」頁面調整。

### 一般

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `uiLang` | `tr` | 介面語言：`tr`、`en`、`de`、`es`、`zh` |
| `autoLaunch` | `false` | 隨 Windows 啟動 |
| `preventSleep` | `true` | 有工作執行時不讓電腦睡眠 |
| `sessionTimeout` | `never` | 閒置這麼多分鐘後中斷連線。背景工作不會重設計時器，只有你的操作會 |

### 卡牌收集

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `autoNextGame` | `true` | 一款完成後換下一款 |
| `cardMaxGames` | `32` | 同時開啟的遊戲數 |
| `fastMinPlaytimeMin` | `120` | 快速模式略過低於此時數的遊戲 |
| `pauseFarmOnBoost` | `false` | 時數提升啟動時停止卡牌收集 |

### 市集

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `priceRefreshHours` | `24` | 取得的價格多久之內仍算新鮮 |
| `historyRefreshHours` | `72` | 成交平均價多久之內仍算新鮮 |
| `fetchAvgWithPrice` | `true` | 取得價格時一併取得平均價。關閉時每件物品一次請求，平均價只能透過「平均」按鈕取得 |
| `bookDepth` | `5` | 詳情面板中掛單簿的列數 |

### 時數提升

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `boostMaxGames` | `32` | 同時開啟的遊戲數 |
| `boostDurationSec` | `3600` | 時段長度 |
| `boostSync` | `false` | 把選取的遊戲拉到共同總時數 |
| `boostSyncMode` | `highest` | 目標：已選取中最高、手動輸入時數，或遊戲庫中最高 |
| `boostSyncStrategy` | `parallel` | `parallel` 為全部同時，`staged` 為逐一進行 |
| `boostAutoRestart` | `false` | 時段結束後重新啟動佇列 |
| `rememberBoostList` | `false` | 在不同時段之間保留選取內容 |

### 擬真模式

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `grDurationSec` | `7200` | 時段長度 |
| `grModel` | `linear` | 分佈模型 |
| `grTcOyun` | `{}` | 各遊戲的 100% 完成時數，以小時計 |
| `grCatchUp` | `true` | 把落後的部分壓縮到時段開頭 |
| `grHiz` | `1` | 整份排程的速度倍率 |
| `grUltraCarpan` | `3` | 低於 5% 的成就要多等幾倍 |
| `grTelafiPay` | `20` | 補齊落後所佔時段的百分比 |
| `grBitmisSik` | `50` | 已完成的遊戲，排程壓縮到多少比例 |
| `grKeepHours` | `true` | 成就解鎖完畢後繼續累積時數 |
| `grSkipUltraRare` | `false` | 完全略過低於 5% 的成就 |

### 隱私

| 鍵值 | 預設 | 作用 |
|---|---|---|
| `offlineMode` | `false` | 執行期間顯示為離線 |
| `hideGameName` | `false` | 顯示你在線上，但不顯示在玩什麼 |

> 顯示為離線會改變好友看到的內容，也可能改變 Steam 是否把你算成正在遊玩。長時間依賴這項設定前，請先測試。

### 設定存放位置

一般設定在 `settings/settings.json`。屬於單一帳號的一切，包括時數提升的選取內容、擬真模式的佇列與預設、成就紀錄與統計，都在 `settings/accounts/<steamID>.json`。快取分開存放於 `cache/`，可以隨時刪除而不會遺失設定。

---

## 🔧 疑難排解

### 應用程式開啟後立刻關閉

已經有另一份在執行。SteamEdge 只允許單一執行個體。請到工作管理員找 `SteamEdge.exe` 並先關閉它。

### 帳號標章一直空白，什麼都載入不了

Steam 工作階段沒有建立起來。連線中斷或正在重試時，上方列下面會出現一條橫幅。若持續如此，先確認 Steam 本身可連線，再查看 `cache/steamedge.log` 找原因。

### 顯示「還剩 40 張卡」，但只掉了幾張

卡牌只有在遊戲累積滿兩小時總時數後才會掉，而且每款遊戲可掉的數量有限。若一整段長時間執行的都是不到兩小時的遊戲，結果會是完全沒有產出；請改用快速模式，它只挑選已超過門檻的遊戲。

### 價格顯示破折號，或填得非常慢

Steam 每個帳號大約允許 30 秒 20 次市集請求，並由價格、成交平均價與掛單共用。庫存龐大時本來就會慢。開啟 `fetchAvgWithPrice` 後每件物品要兩次請求，因此整份庫存填滿的時間會加倍，但你不必等第二輪才看到平均價。

### 某個成就解鎖不了

不是它受保護（由遊戲伺服器寫入、任何客戶端都不得更動），就是該遊戲不透過此協定保存統計。兩種情況都會被辨識並回報，而不會反覆重試。批次作業在連續三次失敗後會停止並說明原因，而不是看起來像卡住。

### 擬真模式只建議解鎖一兩個成就

完成時數設得太高。留空時會依你的遊玩時數推估，因此你玩很久的遊戲會被當成極長的遊戲。請在主面板的欄位填入真正的 100% 完成時數。

### 為回報問題收集紀錄檔

紀錄檔是執行檔旁的 `cache/steamedge.log`，也可以從「設定」開啟。它記錄連線事件、佇列決策與錯誤，**不會**包含你的密碼或工作階段權杖，因此附上是安全的；不過發佈前仍請先大致看過。

---

## ❓ 常見問題

<details>
<summary><b>需要 Steam 客戶端嗎？</b></summary>

不需要，而且它永遠不會啟動客戶端。

</details>

<details>
<summary><b>可以同時使用多個帳號嗎？</b></summary>

可以。每個帳號各自維持自己的連線與資料，你在看某一個帳號時，背景中的帳號會繼續運作。

</details>

<details>
<summary><b>有自動更新嗎？</b></summary>

刻意沒有。應用程式只讀取已發行的版本號，有新版時告訴你。它不下載任何東西，也不修改任何東西。

</details>

<details>
<summary><b>為什麼全部都用我的錢包貨幣顯示？</b></summary>

因為 Steam 就是這樣傳來的。換算等於自行編造匯率。

</details>

<details>
<summary><b>可以把設定搬到另一台電腦嗎？</b></summary>

複製整個資料夾即可，所有東西都在裡面。請記得 `settings/` 內含你的工作階段權杖，複製時請留意隱私。

</details>

---

## 📕 名詞解釋

| 名詞 | 意義 |
|---|---|
| **AppID** | Steam 給每款遊戲的數字識別碼，例如 Cyberpunk 2077 是 1091500 |
| **徽章頁面** | Steam 上列出某款遊戲還剩幾次掉卡機會的頁面 |
| **掉落** | 因遊玩時數而獲得的集換式卡牌 |
| **market_hash_name** | 市集為某件物品使用的精確名稱 |
| **掛單簿** | 某件物品即時的收購單與出售掛單一覽表 |
| **受保護的成就** | 只有遊戲伺服器能設定、任何客戶端都不能設定的成就 |
| **成交** | 已完成的交易，相對於仍在架上的掛單 |
| **結構描述** | Steam 對一款遊戲的成就與統計的定義 |
| **工作階段權杖** | 讓你保持登入狀態的憑證。請當成密碼看待 |
| **Tc** | 100% 完成時數：把遊戲連同所有成就完成所需的小時數 |

---

<div align="center">

[回到 README](./README_ZH.md) · [回報問題](https://github.com/Miabeyefendi/SteamEdge/issues/new?template=bug_report.yml)

</div>
