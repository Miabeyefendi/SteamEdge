<div align="center">

# 📖 SteamEdge 教程

[English](./TUTORIAL.md) · [Türkçe](./TUTORIAL_TR.md) · [Deutsch](./TUTORIAL_DE.md) · [Español](./TUTORIAL_ES.md) · **简体中文** · [Русский](./TUTORIAL_RU.md)

[返回 README](./README_ZH.md) · [更新日志](./CHANGELOG.md)

</div>

---

## 📑 目录

- [总览](#-总览)
- [安装](#-安装)
- [界面导览](#️-界面导览)
- [功能参考](#-功能参考)
- [配置参考](#️-配置参考)
- [故障排查](#-故障排查)
- [常见问题](#-常见问题)
- [术语表](#-术语表)

---

## 🔭 总览

### 它做什么

SteamEdge 让你的 Steam 游戏保持"运行中"，却不真的运行它们。它收集集换式卡牌、累积游戏时长、读写成就，并按真实市场为你的库存估值。这些事情通常都需要 Steam 客户端开着；在这里一个都不需要。

### 它怎么运作

程序说的是 Steam 自己的网络协议，和客户端用的是同一套。它用会话令牌登录，告诉 Steam 哪些游戏正在被玩，然后读回徽章页面、库存、市场数据和成就架构。

由此产生两个结论，它们解释了这个程序绝大部分的行为：

- **Steam 是唯一的事实来源。** 没有任何东西是估算或编造的。如果某个数字取不到，格子里显示的是一个短横，而不是一个猜测。
- **Steam 的限制就是程序的限制。** 市场请求被限制在每个账号每 30 秒约 20 次，程序中所有涉及市场的部分共用这一份额度。卡牌只有在一款游戏总时长超过两小时之后才开始掉落。这些是实测到的事实，不是可调的设置。

### 文件结构

所有东西都放在可执行文件旁边。不会往注册表、`AppData` 或 `Program Files` 里写任何内容。

```
SteamEdge/
  SteamEdge.exe
  settings/
    settings.json              通用设置
    accounts.json              已保存的账号
    session.json               当前会话令牌
    accounts/<steamID>.json    按账号的数据：队列、预设、统计
  cache/
    prices.json                市场价格，有效期 24 小时
    history.json               成交均价，有效期 72 小时
    basarimsiz.json            已确认没有成就的游戏
    steamedge.log              报告问题时要附上的日志
```

> **`settings/` 是敏感目录。** `session.json` 里的令牌足以让人使用你的账号。不要把它放进你要分享的备份、要上传的压缩包或者截图里。

---

## 📦 安装

### 环境要求

Windows 10 或更新版本，64 位。一个启用了 Steam 令牌的 Steam 账号。解压后约占 320 MB 磁盘空间。不需要 Steam 客户端，而且程序从不启动它。

### 分步说明

1. 从[发行版页面](https://github.com/Miabeyefendi/SteamEdge/releases/latest)下载最新的 `.rar`。
2. 解压到你自己的目录。不要放进 `Program Files`，因为程序会把设置写在自己旁边。
3. 运行 `SteamEdge.exe`。
4. 登录。二维码那一栏更省事：用 Steam 手机应用扫码并确认。密码那一栏需要用户名、密码和一个 Steam 令牌验证码。

### 验证安装

会话一旦建立，窗口左下角会显示 `SYSTEM: RUNNING`，右上角的账号标识会填上你的名字、头像和等级。如果标识一直空着，说明会话没有建立起来，见[故障排查](#-故障排查)。

### 升级

程序会核对已发布的版本号，有更新时告诉你。它刻意不下载、也不安装任何东西。要升级，把新压缩包解压覆盖到旧目录上，保留你的 `settings/` 和 `cache/` 目录。

### 卸载

删掉目录。整个流程就这一步。

---

## 🖥️ 界面导览

### Overview

首页。**Active Task** 面板显示真正在跑的任务，一次一个，同时跑多个时可以用箭头切换。Start、Stop 和 Details 作用于你正在看的那个任务，而不是某个固定页面。

下方是队列和活动记录。右侧是统计磁贴：卡牌、库容量、库存价值、时长累积器和成就。某个磁贴显示短横，说明它对应的页面还没加载过，这是在陈述取过什么数据，而不是在陈述你的账号。

### Card Farming

还有卡牌未掉落的游戏列表，取自你的徽章页面。选一个模式，设定单次时长，按 Start。

### Inventory & Market

你的 Steam 库存，合并显示，重复物品算作一行。价格和成交均价在后台逐件获取，两个值一起取。详情面板显示挂单簿，也可以在这里把物品挂出去卖。

### Hours Booster

你的整个游戏库，可搜索。选中游戏，设定并发上限和时长，按 Start。可选的时长同步会把选中的游戏拉到同一个总时长。

### Realistic Mode

三栏工作区。左边是队列和单次时长，中间是解锁顺序，右边是设置，分成 Simple 和 Advanced 两块面板。

### Achievements

按游戏显示从协议读到的真实锁定与解锁状态。选中成就后可以批量解锁或重新锁上，带实时进度，停止按钮在等待过程中也能立刻生效。

### Settings

所有可以对程序下达的设定，按组划分：通用、卡牌farm、市场、时长累积器、真实模式、隐私、账号身份、备份。

### 聊天

从右上角的聊天按钮打开，不在侧边栏。左边是好友，在线的排在前面；右边是对话。Enter 发送，Shift+Enter 换行。未读数量会显示在好友那一行，也会显示在顶栏按钮上。

---

## 🧩 功能参考

### 卡牌 farm

一款游戏总时长不到**两小时**，Steam 就不会掉卡。除一个模式之外，其余模式都不理会这一点，只是把游戏跑起来；**Fast 模式**知道这件事，它只轮换已经越过门槛的游戏，免得把时间花在还掉不出东西的游戏上。

各模式：

| 模式 | 做什么 |
|---|---|
| Sequential | 一次一款，按列表顺序 |
| Most cards | 剩余卡牌最多的游戏优先 |
| Fewest cards | 最接近完成的游戏优先 |
| Priority | 你自己排的顺序 |
| Fast | 只跑已超过两小时的游戏，短间隔轮换 |

卡牌不按固定时间到，Steam 也不会发送"掉了一张卡"这样的事件。程序定期测量剩余卡牌总数，报告的是诚实的差值，而不是一个编出来的计数器。

### 时长累积器

最多同时开 32 款游戏。Steam 对每一款打开的游戏分别计时，所以 32 款游戏开一小时，就是 32 小时游戏时长。

**时长同步**把选中的游戏拉到同一个总时长。两种方式：

- **全部同时** - 所有选中的游戏一起跑，各自达标后陆续退出。这是可能的最快路径：整件事的耗时等于落后最多的那款游戏。
- **逐个进行** - 落后最多的那款先单独往前赶，追上下一款之后一起继续。更慢，但过程中各款游戏能保持齐平。

进度条共用一条时间轴：进度等于一减去该游戏剩余时间除以整件事的总时长。在一次三十五小时的任务中四小时就完成的游戏，起步时进度条几乎是满的；一直跑到最后的那款则从空开始。每一款都恰好在达标的那一刻走到 100%。

### 成就

成就是通过协议读写的，不是抓取你的公开资料页。资料设为私密不影响。

有两类碰不了，程序会从架构中识别出来，而不是反复失败：

- **受保护的成就**由游戏服务器写入。任何客户端尝试写入，Steam 都会拒绝。
- **在此协议下没有统计数据的游戏**（一些大型多人游戏）会报 `0 / N`。这是正确结果，不是故障。

有些游戏只在游戏开着的时候才接受成就写入。程序会为写入打开该游戏，然后恢复之前正在跑的内容。

### Realistic Mode

保持一款游戏开着，在整段时间里逐步解锁它的成就，从最常见的到最稀有的。意义在于它留下的痕迹：一分钟内冒出几百个成就，在个人资料页和第三方站点上都非常扎眼。

**100% 完成时长**是整个页面赖以运作的数字：把这款游戏连同全部成就打完需要多少小时。填进去之后会为该游戏记住。留空则按游戏类型估算，但那个估算是用你自己的游戏时长乘以一个系数，所以在你玩了很久的游戏上会虚高。

**目标数量**由两部分算出：按你的游戏时长本应解锁的数量减去实际已解锁的数量，再加上本次时段应有的份额。面板会把这道算式摊开，方便你核对。

**分布模型**决定间隔的形状。Linear 是均匀的，exponential 前重后轻，像真实玩家最初几小时的样子，Pareto 把大部分集中在前五分之一。

**节奏**按稀有度加权。只有低于 5% 的成就会明显多等一会儿；高于这个比例的都保持均匀而轻快的节奏。玩了超过完成时长的游戏会把整个排程压缩，因为已经没有可供模仿的学习曲线了。

**没有成就的游戏**一旦被发现就会从队列里移除，记录到 `cache/basarimsiz.json`，并且不再出现在这个页面。Steam 公开的库标记并不可靠，只有架构请求才可靠。

### 库存与市场

物品价值取的是**按数量加权的成交价中位数**，而不是最低的在售挂单。某个人把一张卡挂到 999999，撼动不了它。

价格以你账号的**钱包货币**返回，并原样显示。这里刻意不做换算：换算就意味着编造一个汇率。

价格和成交均价是**按物品、成对获取**的，然后队列才移到下一件。两者共用 Steam 那一份市场额度，而限制是按请求数算的，不是按物品数。

### 聊天

好友消息走的是和这里其他功能一样的网络协议，不需要 Steam 客户端参与。打开对话会在 Steam 那边标记为已读，对方也会看到正在输入的提示。

**群组聊天不在范围内。** 它在协议里是另一套概念（chat room groups），需要一个单独的界面。

### 多账号

可以同时连接多个账号。每个账号有自己的引擎、自己的队列和自己的数据文件。切换账号不会重启程序，也不会打断其他账号正在做的事。

---

## ⚙️ 配置参考

设置放在 `settings/settings.json`。下面所有项都能从 Settings 页面里改。

### 通用

| 键名 | 默认值 | 作用 |
|---|---|---|
| `language` | `tr` | 界面语言：`tr`、`en`、`de`、`es`、`zh`、`ru` |
| `autoLaunch` | `false` | 随 Windows 启动 |
| `preventSleep` | `true` | 有任务在跑时不让机器休眠 |
| `sessionTimeout` | `never` | 空闲这么多分钟后断开。后台任务不会重置这个计时，只有你的操作会 |

### 卡牌 farm

| 键名 | 默认值 | 作用 |
|---|---|---|
| `autoNextGame` | `true` | 一款结束后自动换下一款 |
| `cardMaxGames` | `32` | 同时开着的游戏数 |
| `fastMinPlaytimeMin` | `120` | Fast 模式忽略低于这个时长的游戏 |
| `pauseFarmOnBoost` | `false` | 时长累积器启动时停止 farm |

### 市场

| 键名 | 默认值 | 作用 |
|---|---|---|
| `priceRefreshHours` | `24` | 取到的价格保持新鲜多久 |
| `historyRefreshHours` | `72` | 成交均价保持新鲜多久 |
| `fetchAvgWithPrice` | `true` | 与价格同一趟取均价。关闭则每件物品一次请求，均价只能靠 Average 按钮 |
| `bookDepth` | `5` | 详情面板里挂单簿显示几行 |

### 时长累积器

| 键名 | 默认值 | 作用 |
|---|---|---|
| `boostMaxGames` | `32` | 同时开着的游戏数 |
| `boostDurationSec` | `3600` | 单次时长 |
| `boostSync` | `false` | 把选中的游戏拉到同一总时长 |
| `boostSyncMode` | `highest` | 目标：选中项里的 `highest`、`manual` 指定小时数，或整个 `library` 的最高值 |
| `boostSyncStrategy` | `parallel` | `parallel` 是全部同时，`staged` 是逐个进行 |
| `boostAutoRestart` | `false` | 本次结束后重新开始队列 |
| `rememberBoostList` | `false` | 在两次之间保留选中项 |

### Realistic Mode

| 键名 | 默认值 | 作用 |
|---|---|---|
| `grDurationSec` | `7200` | 单次时长 |
| `grModel` | `linear` | 分布模型 |
| `grTcOyun` | `{}` | 每款游戏的 100% 完成时长，单位小时 |
| `grCatchUp` | `true` | 把积压的欠账压缩到本次开头 |
| `grHiz` | `1` | 整个排程的速度倍率 |
| `grUltraCarpan` | `3` | 低于 5% 的成就要多等多久 |
| `grTelafiPay` | `20` | 追赶爆发占本次时长的百分比 |
| `grBitmisSik` | `50` | 已通关的游戏排程压缩到什么程度 |
| `grKeepHours` | `true` | 解锁做完之后继续累积时长 |
| `grSkipUltraRare` | `false` | 完全跳过低于 5% 的成就 |

### 隐私

| 键名 | 默认值 | 作用 |
|---|---|---|
| `offlineMode` | `false` | 运行期间显示为离线 |
| `hideGameName` | `false` | 显示你在线，但不显示在玩什么 |

> 显示为离线会改变好友看到的内容，也可能改变 Steam 是否把你算作正在游戏，所以在依赖它跑长时段之前先测一下。

### 设置保存在哪里

通用设置在 `settings/settings.json`。所有属于某个账号的东西，包括时长累积器的选中项、Realistic Mode 的队列和预设、成就记录和统计，都在 `settings/accounts/<steamID>.json`。缓存是分开的，在 `cache/` 下，随时可以删除而不会丢失配置。

---

## 🔧 故障排查

### 程序一开就关

已经有另一个副本在运行。SteamEdge 只允许一个实例。在任务管理器里找到 `SteamEdge.exe` 先关掉它。

### 账号标识一直空着，什么都加载不出来

Steam 会话没有建立起来。连接断开或正在重试时，顶栏下方会出现提示条。如果一直这样，先确认 Steam 本身能连上，再看 `cache/steamedge.log` 里的原因。

### 写着"还剩 40 张卡"，却只掉了几张

卡牌只有在游戏总时长超过两小时之后才会掉，而且每款游戏的掉落次数本身就有限。如果一整段时间跑的全是不足两小时的游戏，那什么都不会掉；用 Fast 模式，它只挑已经过了门槛的游戏。

### 价格显示短横，或者填得非常慢

Steam 每个账号每 30 秒只允许约 20 次市场请求，而且价格、成交均价和挂单共用这份额度。库存大就是会慢，这是设计使然。开启 `fetchAvgWithPrice` 时每件物品要两次请求，所以整个库存耗时翻倍，但你不用再等第二轮来补均价。

### 某个成就解锁不了

要么它是受保护的，也就是由游戏服务器写入、任何客户端都不能碰；要么这款游戏在此协议下不记录统计数据。这两种情况都会被识别并告知，而不是反复重试。批量操作在连续失败三次后会停下并说明原因，而不是看起来像卡死了。

### Realistic Mode 只建议解锁一两个

完成时长设得太高了。留空时它按你的游戏时长估算，所以一款你玩了很久的游戏会被读成一款长得离谱的游戏。请在主面板的输入框里填上真实的 100% 完成时长。

### 为报告问题收集日志

日志是可执行文件旁边的 `cache/steamedge.log`，也可以从 Settings 里打开。它记录连接事件、队列决策和错误。它**不**包含你的密码或会话令牌，所以附上是安全的；不过发出去之前还是扫一眼。

---

## ❓ 常见问题

<details>
<summary><b>需要 Steam 客户端吗？</b></summary>

不需要，而且它从不启动客户端。

</details>

<details>
<summary><b>能同时跑多个账号吗？</b></summary>

可以。每个账号有自己的连接和数据，你在看某一个账号时，后台的其他账号照常工作。

</details>

<details>
<summary><b>有自动更新吗？</b></summary>

没有，这是刻意的。程序读取已发布的版本号并在有更新时告诉你。它不下载任何东西，也不修改任何东西。

</details>

<details>
<summary><b>为什么都是我的钱包货币？</b></summary>

因为 Steam 就是这么给的。换算就意味着编造一个汇率。

</details>

<details>
<summary><b>能把我的配置搬到另一台机器吗？</b></summary>

把目录复制过去就行，东西全在里面。注意 `settings/` 里有你的会话令牌，所以要私下复制。

</details>

---

## 📕 术语表

| 术语 | 含义 |
|---|---|
| **AppID** | Steam 给游戏的数字编号，例如 1091500 对应赛博朋克 2077 |
| **徽章页面** | Steam 上标明某款游戏还剩多少次掉卡的页面 |
| **掉落** | 因游戏时长而发放的一张集换式卡牌 |
| **market_hash_name** | 市场用来标识一件物品的准确名称 |
| **挂单簿** | 某件物品的实时求购与在售挂单表 |
| **受保护的成就** | 只有游戏服务器能设置的成就，任何客户端都不行 |
| **成交** | 已完成的交易，区别于还挂着的在售条目 |
| **架构** | Steam 对一款游戏的成就与统计数据的定义 |
| **会话令牌** | 让你保持登录状态的凭据。请当作密码对待 |
| **Tc** | 100% 完成时长：把一款游戏连同全部成就打完所需的小时数 |

---

<div align="center">

[返回 README](./README_ZH.md) · [报告问题](https://github.com/Miabeyefendi/SteamEdge/issues/new?template=bug_report.yml)

</div>
